package mr_test

import (
	"testing"
)

func TestCoordinator(t *testing.T) {

	t.Run("test HandleWorker", func(t *testing.T){
		t.Helper()

		// files := []string {"A", "B", "C", "D"}
		// n_reduce := 10
		// // mock a local receiver

		// // dispatch map task to receiver
		// m := mr.MakeCoordinator(files, n_reduce)


	})
}